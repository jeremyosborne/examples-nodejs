// When attempting to require a folder with an index.js package located
// in it, node will attempt to load the index.js file.

module.exports.theanswer = function() {
    return "but what's the question?";
};
